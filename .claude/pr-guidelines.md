Generated by the pr-checklist skill. Edit freely.

## Commands

| Task | Command |
|---|---|
| Package manager | npm |
| Type-check | npm run check (astro check) |
| Unit tests | none for v1 - see below |
| E2E tests | none for v1 - see below |
| Screenshots | none automated - screenshots are manually captured from the Geode app (~/projects/geode), not generated here |

## Coverage Requirements

No unit or E2E test suite for v1. This is a static, content-only marketing +
docs page with no interactivity beyond CSS/anchor scrolling and a
clipboard-copy button. Revisit this decision (and add Playwright) if the site
grows interactive features - forms, client-side state, dynamic content, etc.

## Visual Verification

Whenever any UI is touched, manually verify at these viewports before opening
the PR:

- Desktop: 1280x800
- Mobile: 390x844 (iPhone 14)

For each viewport, confirm:
- Layout is not broken (no overflow, no collapsed sections)
- Interactive elements are reachable and usable (nav anchors scroll to the
  right section, copy-to-clipboard button in Install steps works)
- No visual regressions from the diff
- All 5 screenshots load and are legible (screenshot-vault-editor,
  screenshot-reading-view, screenshot-graph-view, screenshot-search,
  screenshot-command-palette)
- Custom component classes in global.css live inside @layer components -
  plain/unlayered CSS silently wins over Tailwind utilities like `hidden`
  regardless of source order (a real bug hit on the sibling claude-threads-site
  build - keep new component classes inside @layer components too)

## Docs Location

Docs content lives in `src/content/docs/**/*.md`, grouped into five
categories (`overview`, `core-app`, `plugins-api`, `formats-platform`,
`architecture-decisions`) defined in `src/lib/docsCategories.ts`. Source
material is the Geode app repo (`~/projects/geode`): `README.md` for
homepage copy, `docs/spec/*.md` for the core-app/plugins-api/formats-platform
docs, and `docs/adr/*.md` for the architecture-decisions docs. Keep doc
content faithful to the source - light editing (frontmatter, internal link
fixes) is fine, but don't invent new claims about features that aren't in the
source spec/README.

## Screenshot Regeneration

Not automated. The 5 screenshots under `src/assets/screenshots/` (and the
copy at `public/screenshots/screenshot-vault-editor.png` used for the OG
image) must be manually captured from a running Geode build and dropped in
at these exact filenames:

- screenshot-vault-editor.png
- screenshot-reading-view.png
- screenshot-graph-view.png
- screenshot-search.png
- screenshot-command-palette.png

Recommended size: 1440x900 or similar aspect ratio (matches the placeholder
generator used to bootstrap this repo). There is no script for this - capture
manually, review each shot at both verification viewports, and replace the
file directly. Re-run `npm run build` after replacing to confirm nothing
about layout depends on placeholder dimensions.

## Project-Specific Gates

- `astro check` must be clean (zero errors) - enforced as part of `npm run build`.
- `astro build` must complete with zero errors before opening a PR.
- Known limitation: no custom 1200x630 OG image has been designed yet; the
  OG/Twitter card reuses `screenshot-vault-editor.png` as a placeholder.

## Changelog

The `/changelog/` page content (`src/content/changelog/*.md`) is generated by
`npm run changelog:generate` (wraps `scripts/generate-changelog.ts`), which
pulls every GitHub Release from the Geode app repo (`rbcodelabs/geode`) via
`gh api ... --paginate` and writes one Markdown file per release, verbatim.
It requires the `gh` CLI to be authenticated (`gh auth status`). Like the
screenshots, this is NOT run automatically as part of the build - the site is
`output: 'static'` and must not depend on live GitHub API calls at build
time. Re-run it manually whenever a new Geode version ships (to pick up the
new release) or to pull in edits to existing release notes on GitHub.
