---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<article class="geode-feature-card card">
  <h3>{title}</h3>
  <p>{description}</p>
</article>
